version : "2"

services:
  www:
    build: .
    image: nginx/log
    ports:
      - "9090:8080"
    volumes:
      - /var/jenkins/buildlog:/usr/share/nginx/www/logs
    container_name: listoflog
    restart: always
